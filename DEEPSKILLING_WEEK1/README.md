Inventory, search, sorting, and data-structure exercises (Java)

Compile:

```bash
javac src/*.java
```

Run examples:

```bash
java -cp src InventoryManagement
java -cp src EcommerceSearch
java -cp src OrderSorting
java -cp src EmployeeManagement
java -cp src TaskManagement
java -cp src LibraryManagement
java -cp src FinancialForecasting
```

Each file demonstrates the requested data structures, algorithms, and includes simple examples and analysis comments.
